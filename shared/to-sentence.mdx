Converts an array of strings to a comma-separated sentence

## Parameters

| Parameter | Type                  | Description     |
| --------- | --------------------- | --------------- |
| `items`   | <code>string[]</code> | {Array<string>} |

## Returns

`string` — Returns a string with the items joined by a comma and the last item joined by ", or"
