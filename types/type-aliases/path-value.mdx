> **PathValue**\<`T`, `P`\>: `P` *extends* `` `${infer Key}.${infer Rest}` `` ? `Key` *extends* keyof `T` ? `Rest` *extends* [`RecordToPath`](record-to-path.mdx)\<`T`\[`Key`\]\> ? [`PathValue`](path-value.mdx)\<`T`\[`Key`\], `Rest`\> : `never` : `never` : `P` *extends* keyof `T` ? `T`\[`P`\] : `never`

Used to read the value of a string path inside an object defined as const
const obj =  { a: { b: 'hello' }}  as const;
type Value = PathValue<typeof obj, 'a.b'>
Value is now a union set containing a single type: 'hello'

## Type Parameters

| Type Parameter |
| ------ |
| `T` |
| `P` *extends* [`RecordToPath`](record-to-path.mdx)\<`T`\> |
